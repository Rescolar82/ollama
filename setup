from setuptools import setup, find_packages

setup(
    name='ollama',
    version='0.1',
    packages=find_packages(),
    install_requires=[],  # Aquí puedes especificar las dependencias si las hay.
)
